// 4. Sum of even factors under the number limit
#include<stdio.h>

int sum_of_even_factors(int number)
{
    // TODO: return sum of all positive even factors excluding 'number'
    return 0;
}

int main()
{
    int number;
    printf("Enter number: ");
    scanf("%d", &number);
    printf("%d", sum_of_even_factors(number));
    return 0;
}