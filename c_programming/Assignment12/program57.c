// 2. Count total factors of a number
#include<stdio.h>

int count_factors(int number)
{
    // TODO: return the total number of positive factors of 'number'
    return 0;
}

int main()
{
    int number;
    printf("Enter number: ");
    scanf("%d", &number);
    printf("%d", count_factors(number));
    return 0;
}